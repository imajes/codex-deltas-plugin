version = 1
id = "codex-delta-report-template"
name = "Codex delta report template"
status = "ACTIVE"
rrule = "RRULE:FREQ=HOURLY;INTERVAL=6;BYMINUTE=0;BYDAY=MO,TU,WE,TH,FR"
execution_environment = "local"
model = "gpt-5.4"
reasoning_effort = "high"
# Prototype only: copy this file into a real automation root and customize `id`, `name`,
# schedule, and any path/env bindings for the target automation.
prompt = "Goal: generate the next repository delta report with the `codex-deltas` plugin.\n\nTreat the following variables as the environment this workflow needs for this run, and pass them through to any invoked skill(s):\n- `$AUTOMATION_NAME`: <AUTOMATION_NAME>\n- `$AUTOMATION_ROOT`: `$CODEX_HOME/automations/$AUTOMATION_NAME`\n- `$REPO_URL`: <REPOSITORY_GIT_URL>\n- `$ARTIFACT_ROOT`: `$CODEX_HOME/config/deltas`\n- `$MEMORY_PATH`: `$AUTOMATION_ROOT/memory.md`\n\nRead and follow [$codex-deltas:orchestrate](skills/orchestrate/SKILL.md) to generate the next repository delta report.\n\nUse the skill to choose the workflow, lane order, and report structure, then execute the documented command surfaces directly as needed."
created_at = 1771972261073
updated_at = 1774481976801
